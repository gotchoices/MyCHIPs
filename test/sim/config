#Configuration parameters for network simulation
#Modify as needed for local configuration

user=root			#Will connect to remote machines with this ID
remsrc="/usr/local/devel"	#Where to replicate source directory on remote machines
vmsrc="~/share/devel"		#Where to find source directory on local vms
tmpdir="/var/tmp/mychips"
public=lux0			#Where our public document database will be running
admin="admin"
dbname="mychips"

locals=(lux0 lux1 lux2 lux3)	#Any machines that already have an up-to-date source mirror (via nfs or the like)
#locals=()

#remotes=( 192.168.5.11 192.168.5.15 192.168.5.16 192.168.5.18 )
#remotes=( 192.168.5.15 192.168.5.16 )
remotes=( )

machines=( "${locals[@]}" "${remotes[@]}" )
